{
  "name": "vyom",
  "version": "0.1.0",
  "private": true,
  "description": "VYOM — Open AI Development Platform",
  "workspaces": [
    "packages/*",
    "packages/plugins/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "dev:desktop": "turbo run dev --filter=@vyom/ui-desktop",
    "dev:cli": "turbo run dev --filter=@vyom/cli"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "prettier": "^3.2.0",
    "eslint": "^9.0.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
